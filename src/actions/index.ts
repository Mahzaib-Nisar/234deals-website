/**
 * Server actions should live here. Keep actions thin and focused
 * on data orchestration. Do not include UI logic here.
 */

export async function exampleServerAction() {
  // placeholder for server-side operations (DB, auth, etc.)
  return null;
}

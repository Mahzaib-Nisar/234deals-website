import { getDeals as coreGetDeals, createDeal as coreCreateDeal, updateDeal as coreUpdateDeal, deleteDeal as coreDeleteDeal } from "@/lib/api";

export const getDeals = coreGetDeals;
export const createDeal = coreCreateDeal;
export const updateDeal = coreUpdateDeal;
export const deleteDeal = coreDeleteDeal;
